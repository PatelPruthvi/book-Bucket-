import 'package:bookbucket/Utils/utils.dart';
import 'package:flutter/material.dart';

class Discover extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: dark_blue_1,
        elevation: 5,
        title: Text('Discover'),
      ),
      body: Center(
        child: Text('Discover'),
      ),
    );
  }
}
